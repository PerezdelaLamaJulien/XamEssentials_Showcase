<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamEssentials_Showcase.Features.Pages.DetectShakePage"
             Title="Detect Shake">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center">
            <Label Text="Lancer la détection"
                   HorizontalOptions="Center"/>

            <Switch IsToggled="{Binding SwitchIsToggled}"
                    HorizontalOptions="Center"/>

            <Label Text="{Binding ShakeString}"
                   HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>