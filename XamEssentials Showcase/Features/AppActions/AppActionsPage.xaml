<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamEssentials_Showcase.Features.Pages.AppActionsPage"
             Title="App Actions">
    <ContentPage.Content>
        <StackLayout VerticalOptions="Center"
                     Padding="15">
            <Button Text="Create Default Actions"
                    Command="{Binding CreateDefaultActionCommand}"
                    HorizontalOptions="Center"/>

            <Entry Text="{Binding ActionText}"
                   HorizontalOptions="Fill"
                   Placeholder="Nom de l'action"/>

            <Button Text="Create Custom Actions"
                    Command="{Binding CreateCustomActionCommand}"
                    HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>